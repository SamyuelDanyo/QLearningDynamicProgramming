!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
!! 1. MAKE SURE "task1.mat" and "qeval.mat" reward files are in < master/src >
!! 2. The code displays figures & asks about evaluation input.
!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
Dependencies:
------------
    Python --version >= 3.7
    Python Standard Library: (time, warnings, os, sys, operator)
    Third Party: (NumPy, MatPlotLib, scipy.io)

    Recommended:
        Please make sure that you have installed Anaconda Python (version >= 3.7) in your environment.
        Information on installing Anaconda:
        < https://www.anaconda.com/distribution/ >
        < https://docs.anaconda.com/anaconda/install/windows/ >
        < https://docs.anaconda.com/anaconda/install/linux/ >

    Secondary:
        If you already have Python &/or do not want to install a full distribution:
        Make sure your environment satisfies all the requirements/dependencies listed above.
        
Interactive
-----------
The project is also provided in a Jupyter Notebook, which is a web-based interactive
Python interpreter that allows users to type and run Python code in a web browser.
An example execution is exported to html notebook "QLearningDynamicProgramming.html" and can be found in <master/docs>
Please make sure that you satisfy the above-mentioned requirements, Jupyter Notebook will
be installed along with Anaconda.

1. Open Anaconda Prompt (or your Python environment prompt).
2. Navigate to root/src/, home of QLearningDynamicProgramming.ipynb
3. Execute < jupyter notebook >
4. Click on the "QLearningDynamicProgramming" notebook.
5. Start running the blocks of code sequentially either by clicking the < run cell, select below > button
   or by keys < shift + Enter > while selected the block of code.

Python Command
---------------
Make sure you Python is installed on your command line.
	//////////////////////////////////////////////////
	// HOW TO THE RUN CODE
	//////////////////////////////////////////////////
	1. Navigate to root/src
	2. < python -W ignore QLearningDynamicProgramming.py >
        3. < python -W ignore QLearningDynamicProgrammingExperiment.py >

Documentation
---------------
For more information on design & implementation, see documentation in < master/docs/QLearningDynamicProgrammingDoc.pdf>